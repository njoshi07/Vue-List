
    const state = {
        counterNum: 2
    };
    const mutations = {};
    const action = {};
    const getters = {
        getCounter: state => state.counterNum,
        getCounterPlus: state => state.counterNum + 2
    };

 export default {
     state,
     mutations,
     action,
     getters
 }
